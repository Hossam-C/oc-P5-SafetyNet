package com.SafetyNet.Alerts;


import com.SafetyNet.Alerts.Model.Firestations;
import com.SafetyNet.Alerts.Model.Medicalrecords;
import com.SafetyNet.Alerts.Model.Persons;
import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.util.ArrayList;



public class readJsonTest {

//    private LoadJson loadJson;

    @Test
    public void testlectureJson() throws IOException {

/*        loadJson = new LoadJson();
        ReadJson readJson = new ReadJson();

        ArrayList<Persons> lpersons = loadJson.readPersons(readJson.read());
        ArrayList<Firestations> lfirestations = loadJson.readFirestations(readJson.read());
        ArrayList<Medicalrecords> lmedicalRecords = loadJson.readMedicalRecord(readJson.read());*/

        System.out.println("Ok");

    }
}
